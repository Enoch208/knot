events {}

http {
  resolver 127.0.0.11 valid=30s ipv6=off;

  server {
    listen 8080;
    server_tokens off;

    location / {
      set $object_store object-store:9000;
      proxy_set_header Host $object_store;
      proxy_pass http://$object_store;
    }
  }
}
